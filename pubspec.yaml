name: simple_appstore_screenshot
description: Flutter Library to create appstore easily in your app easily
version: 0.0.3
repository: https://github.com/lky2431/simple_appstore_screenshot
homepage: https://pub.dev/packages/simple_appstore_screenshot


environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  assets:
    - assets/

